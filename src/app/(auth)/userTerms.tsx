import { ScrollView, Text, View } from "react-native";

export default function () {
  return (
    <ScrollView>
        <View>
            <Text>Termos de uso</Text>
        </View>
    </ScrollView>
  );
}
